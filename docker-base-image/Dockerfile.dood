# Derived image that keeps Solana tool-chain **and** adds the Docker CLI
FROM ghcr.io/sequence4/solana-toolchain:runtime-latest-amd64
RUN apt-get update \
    && apt-get install -y --no-install-recommends docker.io \
    && rm -rf /var/lib/apt/lists/* 