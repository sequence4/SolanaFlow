--- a/server/src/utils/codeGen/handleGenerateCode.ts
+++ b/server/src/utils/codeGen/handleGenerateCode.ts
@@ -6776,8 +6776,19 @@ export const handleGenerateCode = async ({
           workspace,
           projectId,
           /* creatorId */ null
-        );
-      } // ← closes "try {"
-      /*  (very long file continues, but never returned a value)  */
+        );
+
+        /* ──────────────────────────────────────────────────────────────── *
+         * 🎉  Every step succeeded – hand results back to the caller
+         * ──────────────────────────────────────────────────────────────── */
+        return {
+          sentinelId,
+          programName,
+        };
+      } catch (err) {
+        console.error('[GEN] handleGenerateCode error:', err);
+        throw err;                 // preserve stack & satisfy TS
+      }
 };      // ← function ends cleanly
